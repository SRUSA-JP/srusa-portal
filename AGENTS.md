# srusa-portal 作業ガイド

## 目的

- このリポジトリは、MkDocs ベースの SRUSA ポータルサイトを管理します。
- 主な管理対象は、今後追加する Markdown コンテンツ、MkDocs 設定、依存関係、ビルドおよび公開設定です。
- 現在は「ホーム」と「登山部」の入口ページを持つ最小構成です。

## 現在の仕様

### 合意済み

- 静的サイトジェネレーターに MkDocs を使用します。
- 最初のトップレベルナビゲーションは「ホーム」と「登山部」です。
- サイト実装より先に、README とこの作業ガイドを整備します。
- テーマには Material for MkDocs を使用します。
- コンテンツは `docs/` 配下に置き、登山部は `docs/mountaineering/` 配下で管理します。
- Python 依存関係は `requirements.txt` でバージョンを固定します。
- 標準の開発環境には `.devcontainer/` の Dev Container を使用します。Zed と VS Code のどちらからでも利用できる構成を維持します。
- Cloudflare Pages の GitHub 連携を使用し、`main` ブランチを本番へ自動デプロイします。
- Pull Request の Preview は、PR の `preview` コメントまたは GitHub Actions の画面から手動実行し、Cloudflare Pages の Preview URL を対象 Pull Request から開けるようにします。
- Cloudflare Pages での公開確認が完了するまでは、`.github/workflows/deploy-pages.yml` による既存の GitHub Pages デプロイも維持します。
- ローカル、GitHub Actions、Cloudflare Pages の Python バージョンは `.python-version` と Dev Container で 3.11 にそろえます。

### 未決事項

- 各ページに掲載する具体的な内容
- サイト固有の色、ロゴなどのデザイン
- 公開URLと独自ドメインの有無

未決事項を実装する場合は、既存の決定として扱わず、候補と影響を提案してユーザーと確認してください。

## 作業ルール

- ユーザー向けの説明、ドキュメント、コミットメッセージは、特に指定がない限り日本語を使ってください。
- 作業前に、このリポジトリの README、設定、ファイル構成、差分を確認してください。
- 他リポジトリは参考情報として扱い、その配置、命名、依存関係、公開方法をこのリポジトリの規約として流用しないでください。
- 合意済みの内容、提案、未決事項を明確に分けてください。未確認の提案を決定済みとして扱わないでください。
- 目的に必要な範囲で、小さく確認しやすい変更を優先してください。
- ページ追加時は、ナビゲーション設定、実ファイルのパス、リンクの大文字小文字を一致させてください。
- 団体、活動、日程、場所、連絡先、登山ルート、安全情報などの事実を推測で補わないでください。不明な情報は TODO として明示するか、ユーザーへ確認してください。
- 登山に関する案内では、日時によって変わる天候、交通、施設情報を固定的な事実として記載しないでください。

## コミットメッセージ

コミットメッセージは `[prefix]要約` の形式とし、変更の主目的に応じて次のプレフィックスを使用してください。

- `[add]`: ファイルまたは機能の追加
- `[update]`: 既存機能の更新
- `[remove]`: ファイルの削除
- `[clean]`: 動作を変えないリファクタリング

## 構成変更時の方針

- 新しいトップレベルタブやコンテンツ分類は、ページ構成と URL への影響を確認してから追加してください。
- 依存関係を追加するときはバージョン方針を明示し、ローカル環境と CI で同じ定義を使用してください。
- Dev Container の依存関係は `requirements.txt` を参照し、別の定義を重複して持たせないでください。
- GitHub Actions のバージョンを変更する場合は、公式リリースと互換性を確認してください。
- 実行可能な手順を追加したら、README にセットアップ、プレビュー、ビルド、検証の各コマンドを反映してください。
- 生成物は原則としてコミットせず、MkDocs の `site/` は `.gitignore` の対象にしてください。
- 公開設定や CI/CD を追加する場合は、権限、秘密情報、公開先、失敗時の復旧方法を確認してください。

## セキュリティとプライバシー

- token、secret、credential、private key、個人用 `.env`、セッション、ログを追加しないでください。
- 写真、地図、行動記録を追加する場合は、個人情報、位置情報、公開範囲を確認してください。
- 外部サービスの認証情報はリポジトリへ直接書かず、利用する公開基盤の secret 管理を使用してください。

## 変更時の確認

- README の説明と実際のファイル構成、コマンドが一致しているか確認してください。
- Markdown の内部リンクと MkDocs のナビゲーションが解決できるか確認してください。
- MkDocs 導入後は、変更内容に応じてローカルビルドを実行してください。
- 追加ファイルと Git 差分に秘密情報や不要な生成物が含まれていないか確認してください。
