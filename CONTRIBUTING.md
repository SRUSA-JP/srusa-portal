# コントリビューションガイド

このリポジトリへの変更は、MkDocs で管理する小さなポータルを保つことを優先します。

## 変更できるもの

- `docs/` 配下の Markdown コンテンツ
- `mkdocs.yml`のサイト、テーマ、自動ナビゲーション設定
- `requirements.txt` の Python 依存関係
- `.devcontainer/` の共通開発環境
- `.github/workflows/` のビルド・公開設定
- リポジトリ内の開発・運用ドキュメント

React、Node.js、Viteなどを使うアプリケーションや、そのビルド成果物は管理しません。インタラクティブな試験機能は別サービスで管理し、このポータルからは外部リンクで案内します。

## 変更を始める

新しい機能の実装や影響の大きい改修を始める前に、対応する Issue を作成します。既存の Issue に対応する場合は、目的と範囲を確認します。

目的と影響範囲が明確で短時間にレビューできる小規模な変更では、Issueを省略できます。次はその例であり、これらに限定しません。

- ファイル名や表示名の単純なリネーム
- 原因と修正箇所が明確な軽微なバグ修正
- 既存方針を変えない範囲での`mkdocs.yml`の小規模な調整

小規模に見える変更でも、トップレベルのナビゲーションやURL、依存関係、公開先、権限、セキュリティ、プライバシーに影響する場合は、実装前にIssueで影響を確認します。

Issueに対応する場合は、最新の `main` から、`issue/<Issue番号>` という名前のブランチを作成します。

```shell
git switch main
git pull --ff-only
git switch -c issue/123
```

1つのPull Requestには、1つの目的に必要な変更だけを含めます。

例の`123`は実際のIssue番号に置き換えます。ブランチ名には説明文を追加せず、Issue番号だけを使用します。

`docs/`直下に新しいページやディレクトリを追加すると、トップレベルナビゲーションにも自動的に追加されます。依存関係や公開先の追加と同様に、URLや運用への影響をIssueで確認してから実装してください。

## コンテンツを編集する

- ユーザー向けの文章は、特に理由がなければ日本語で記述します。
- ファイル名、`mkdocs.yml` のパス、Markdownリンクでは大文字・小文字も一致させます。
- ナビゲーションは`docs/`の構成から自動生成されます。ディレクトリ名が表示名、ファイル構成が並び順に影響するため、追加・移動後は実際の表示を確認します。
- 表示名や並び順を個別に固定したくなっても、`mkdocs.yml`へ`nav`を再追加する前に、自動生成をやめる影響をIssueで確認します。
- リポジトリ内のページには相対リンク、外部サイトには `https://` から始まるURLを使用します。
- 外部リンクは、リンク先が別サービスだと読者に分かる表記にします。
- 団体、活動、日程、場所、連絡先、登山ルート、安全情報を推測で補いません。不明な内容は確認するか、TODOであることを明記します。
- 天候、交通、施設情報など、日時によって変わる情報を固定的な事実として記載しません。

## セキュリティとプライバシー

次の情報はコミットしないでください。

- token、secret、credential、private key、パスワード
- 個人用の `.env`、セッション、ログ、キャッシュ
- 公開について同意を確認していない個人情報、写真、位置情報、行動記録

Cloudflare Pages Previewは共有Basic認証の対象ですが、資格情報を知る利用者が閲覧できます。また、リポジトリ自体は公開されています。外部公開されて困る情報は含めないでください。

## 検証する

変更後は仮想環境またはDev Containerで次を実行します。

```shell
mkdocs build --strict
git diff --check
```

あわせて次を目視で確認します。

- 変更したページとナビゲーションを開ける
- 内部リンクと外部リンクが意図したURLを指している
- `site/`、仮想環境、ログ、秘密情報が差分に含まれていない
- READMEの手順と実際のファイル・コマンドが一致している

`functions/_middleware.js`を変更した場合は、Node.js 20以降で次も実行します。

```shell
node --test tests/basic-auth.test.mjs
```

## コミットする

コミットメッセージは日本語で、`[prefix]要約` の形式にします。

| prefix | 用途 |
| --- | --- |
| `[add]` | ファイルまたは機能の追加 |
| `[update]` | 既存機能の更新 |
| `[remove]` | ファイルの削除 |
| `[clean]` | 動作を変えない整理 |

変更理由を追いやすい、小さく完結した単位でコミットしてください。生成される `site/` はコミットしません。

変更したファイルだけを明示してステージし、コミットします。次は`docs/index.md`を変更した場合の例です。

```shell
git add docs/index.md
git commit -m '[update]ホームの案内を更新'
```

## Pull Requestを作成する

Pull Requestには次を記載します。

- 変更の目的と対象範囲
- 関連Issue（マージ時に閉じる場合は `Closes #<Issue番号>`）
- 実行した検証と結果
- 未決事項、未実施の確認、運用上の注意

作業ブランチをpushするとCloudflare Pages Previewが自動で作成されます。詳しい確認手順は[運用ガイド](OPERATIONS.md)を参照してください。

コミット後、作業ブランチをpushしてPull Requestを作成します。次は`issue/123`ブランチの例です。

```shell
git push -u origin issue/123
```

1. GitHubでこのリポジトリを開き、`Compare & pull request`を選ぶ
2. baseが`main`、compareがpushした作業ブランチであることを確認する
3. 変更目的、関連Issue、検証結果、未決事項を本文に記載する
4. 内容を確認してPull Requestを作成する
